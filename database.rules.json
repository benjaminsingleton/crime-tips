{
  "rules": {
    "tips": {
      ".read": "auth !== null",
      "$tip": {
        ".write": "!data.exists() || auth !== null"
      }
    },
    "users": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    }
  }
}