{
  "rules": {
    "tips": {
      "$tip": {
        ".read": "auth !== null",
        ".write": "!data.exists() || auth !== null",
      },
    },
    "users": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    }
  }
}